target_sources(v3d_info
  PRIVATE
    common.h
    V3DInfo.cpp
)

if(CURSES_FOUND)
  target_sources(v3d_profile
    PRIVATE
      common.h
      V3DProfile.cpp
  )
endif()

if(VC4CL_BUILD_DUMP_ANALYZER)
  target_sources(vc4cl_dump_analyzer
    PRIVATE
      common.h
      DumpAnalyzer.cpp
  )
endif()